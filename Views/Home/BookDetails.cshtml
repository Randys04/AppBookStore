@model AppBookStore.Models.Domain.Book
@{

}

<div class="movie-datail w-80 m-auto">
    <div class="img-container">
        <img src="/Uploads/@Model.CoverImage" alt="cover">
    </div>
    <div class="detail-container">
        <h3>@Model.Title</h3>
        <h4>@Model.CategoriesNames</h4>
        <h4>@Model.CreateDate</h4>
        <h3>@Model.Author</h3>
    </div>

    <div class="w-80 m-auto">
        <a href="/Home/Index" class="btn btn-default">Atras</a>
    </div>
</div>